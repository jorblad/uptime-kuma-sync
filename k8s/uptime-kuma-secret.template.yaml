# This file is a TEMPLATE ONLY. Do NOT commit real secrets.
# Create a copy of this file outside of the repository, fill in the real values,
# and apply it to the cluster with: kubectl apply -f ~/secrets/uptime-kuma-secret.yaml
#
# Example workflow:
#   cp k8s/uptime-kuma-secret.template.yaml ~/secrets/uptime-kuma-secret.yaml
#   chmod 600 ~/secrets/uptime-kuma-secret.yaml
#   # Edit ~/secrets/uptime-kuma-secret.yaml and replace the placeholder values.
#   kubectl apply -f ~/secrets/uptime-kuma-secret.yaml
# ---
# apiVersion: v1
# kind: Secret
# metadata:
#   name: uptime-kuma-secret
#   namespace: kube-system
# type: Opaque
# stringData:
#   base_url: "https://uptime-kuma.example.com"
#   api_token: "REPLACE_WITH_REAL_TOKEN"
